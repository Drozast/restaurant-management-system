# Server Configuration
NODE_ENV=production
PORT=3001

# Database
# SQLite database will be created automatically in ./data/pizza.db
# No additional configuration needed

# CORS (only needed in development with separate frontend)
# CORS_ORIGIN=http://localhost:3000
